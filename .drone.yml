pipeline:
  docker:
    environment:
      - APP_VERSION="v1.0.0"
    image: plugins/docker
    repo: piterjs/piterjs.org
    dockerfile: Dockerfile
    tags:
      - $APP_VERSION-$DRONE_BUILD_NUMBER
      - latest
    secrets: [ docker_username, docker_password ]
